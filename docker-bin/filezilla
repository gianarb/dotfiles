docker run --rm -it \
       -v /tmp/.X11-unix:/tmp/.X11-unix \
       -e DISPLAY=unix$DISPLAY \
       --rm \
       -v "${HOME}/.filezilla:/root/.filezilla" \
       -v "${HOME}:${HOME}" \
       --name filezilla \
       gianarb/filezilla "$@"
